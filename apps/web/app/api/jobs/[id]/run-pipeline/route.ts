import { runJobPipeline } from "@career-os/domains";
export async function POST(request: Request, { params }: { params: { id: string } }) { const body = await request.json().catch(() => ({})); const result = await runJobPipeline({ id: params.id, title: body.title ?? "Splunk Platform Engineer", company: body.company ?? "Seeded Company", location: body.location ?? "Remote", description: body.description ?? "Splunk Cribl Terraform AWS observability role", source: body.source ?? "api", employmentType: body.employmentType, requiredFields: body.requiredFields, hasEasyApply: body.hasEasyApply }); return Response.json(result); }
