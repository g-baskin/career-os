import { domainRegistry } from "@career-os/domains";
const workspaces = ["Today’s Mission","Jobs","Companies","Applications","Recruiters / Relationships","Email Center","Calendar / Interviews","Resume Factory","Documents","Follow-Ups","Market Intelligence","Skill Gaps","Settings","System Health","Application Packets","Packet Detail","Relationships","Relationship Detail","Job Pipeline Results","Admin / Domain Registry"];
const jobSections = ["Remote Commercial","Hybrid Commercial","Onsite Commercial","Contract","Clearance / Government","Low Fit","Archived / Rejected"];
const appSections = ["Ready to Apply","Awaiting Review","Submitted","Follow-Up Due","Interviewing","Rejected","Offer Received","Closed"];
export default function Page(){return <div className="shell"><aside className="sidebar"><div className="brand">Career OS</div><nav className="nav">{workspaces.map(w=><a key={w} href={`#${w}`}>{w}</a>)}</nav></aside><main className="main"><span className="badge">Phase 1 foundation</span><h1>AI-assisted career operating system</h1><p className="muted">Event-driven dashboard shell with human approval gates; no auto-submit and no LinkedIn scraping.</p><section id="Today’s Mission" className="section"><h2>Today’s Mission</h2><div className="grid">{["top remote commercial jobs","hybrid commercial jobs","onsite commercial jobs","clearance/government separated jobs","low-fit jobs","jobs ready for packet generation","packets awaiting review","follow-ups due placeholder","estimated apply time placeholder"].map(i=><div className="card" key={i}><strong>0</strong><br/><span className="muted">{i}</span></div>)}</div></section><section id="Jobs" className="section"><h2>Jobs</h2><div className="grid">{jobSections.map(s=><div className="card" key={s}>{s}<p className="muted">Segmented, never blindly deleted.</p></div>)}</div></section><section id="Applications" className="section"><h2>Applications</h2><div className="grid">{appSections.map(s=><div className="card" key={s}>{s}</div>)}</div></section><section id="Admin / Domain Registry" className="section"><h2>Domain Registry</h2><div className="grid">{domainRegistry.map(d=><div className="card" key={d.slug}><strong>{d.name}</strong><p className="muted">{d.manager}</p><span className="badge">{d.status}</span></div>)}</div></section></main></div>}
