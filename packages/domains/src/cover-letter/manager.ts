import type { DomainDefinition } from "@career-os/shared";
export const definition: DomainDefinition = { name: "Cover Letter Domain", slug: "cover-letter", manager: "CoverLetterManager", capabilities: ["CoverLetterGenerationCapability"], workers: ["CompanySpecificCoverLetterWorker", "ShortCoverLetterWorker", "NoCoverLetterDecisionWorker"], commands: ["cover_letter.generate_placeholder"], events: ["cover_letter.placeholder_created"], permissions: ["generate_resume"], dependencies: ["application-packet"], status: "phase_1", version: "0.2.0" };
export class CoverLetterManager { readonly definition = definition; createPlaceholder(companyName: string) { return { eventType: "cover_letter.placeholder_created" as const, content: `Cover letter placeholder for ${companyName}; requires review before use.` }; } }
export const definition: DomainDefinition = { name: "Cover Letter Domain", slug: "cover-letter", manager: "Cover Letter Manager", capabilities: ['Placeholder Capability'], workers: ['Placeholder Worker'], commands: ["cover-letter.execute"], events: ["cover-letter.completed"], permissions: [], dependencies: [], status: "placeholder", version: "0.1.0" };
export class CoverLetterManager { readonly definition = definition; }
